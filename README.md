Install the requirements via pip install -r requirements.txt <br />
Old version (doesn't work on all devices) that loops over all devices and gets the hostname and firmware version. <br />
Archived because of new version that works on all devices, contact me for more info.